# Relative paths are resolved from this file's directory.
SESSION_TRUNCATION_BUFFER_DIR=./tmp
SESSION_TRUNCATION_OUTPUT_DIR=~/Downloads

# "local" uses the system time zone, matching cc-switch display timestamps.
# An IANA time zone such as Europe/Brussels is also supported.
SESSION_TRUNCATION_TIMEZONE=local
